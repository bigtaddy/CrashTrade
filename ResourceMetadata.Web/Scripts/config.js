(function(global) {

    'use strict';

    global.CrashTradeSettings = {
        'baseUrl': '/api/',
        'tokenUrl': '/Token',
        'webUrl': 'http://localhost:9043/',
        'tokenKey': 'CRASH_TRADE.ACCESS_TOKEN'

    }
} (window));

